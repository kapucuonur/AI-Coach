services:
  api:
    build: .
    container_name: coachonurai-api
    restart: always
    ports:
      - "8001:8000"
    env_file:
      - .env
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
