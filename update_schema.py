from backend.database import engine, Base
from backend.models import User
# Initialize the updated DB schema logic if any changes were made! Nothing expected since coach.py changes only affect runtime variables
